# Verification evidence

Artifacts written by `.cursor/skills/verify-scripture-outliner/scripts/drive.mjs`. Cleanup does not delete this directory.

First proven feature: [import-sample](./import-sample/). Inline-outline PRs must also keep [inline-outline](./inline-outline/) screenshots (bold header, nested indent, depth ≥ 2, untinted body, `nested-selected.png` with a visible range highlight). Deselect-outside PRs must keep [deselect-outside](./deselect-outside/) `selected.png` (word selected) and `deselected.png` (cleared by a margin tap). Header-tap selection is locked by [header-select](./header-select/). Icon-toolbar PRs must keep [icon-toolbar](./icon-toolbar/) at 390×844 showing the icon hint including X and excluding Section (H). Show-text PRs must keep [show-text](./show-text/) `text-on.png` (checkbox on, body words visible) and `text-off.png` (headers only) at 390×844. Export PRs must keep [export-markdown](./export-markdown/) `export-menu-open.png` plus `outline.md`, [export-headers-only](./export-headers-only/) `outline.md` with no body phrase, and [export-docx](./export-docx/) `outline.docx` starting with PK. Header-body-spacing PRs must keep [header-body-spacing](./header-body-spacing/) `uneven.png` (failing nested gaps) and `nested.png` (even header→body gaps) at 390×844.
